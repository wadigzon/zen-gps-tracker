import 'package:flutter/material.dart';

/// Serene Zen color palette for GPS tracking application.
class ZenColors {
  // Backgrounds
  static const Color background = Color(0xFF0F1722);
  static const Color surface = Color(0xFF1E293B);
  static const Color surfaceLight = Color(0xFF334155);

  // Glass Container Colors
  static const Color glassBackground = Color(0xCC0F172A);
  static const Color glassBorder = Color(0x33F8FAFC);

  // Brand Accents
  static const Color emeraldPrimary = Color(0xFF10B981); // Active tracking / Start
  static const Color emeraldLight = Color(0xFF34D399);
  static const Color amberPause = Color(0xFFF59E0B);     // Paused
  static const Color roseStop = Color(0xFFEF4444);       // Stop
  static const Color cyanAccent = Color(0xFF06B6D4);     // Speed / Altitude
  static const Color goldAccent = Color(0xFFFBBF24);

  // Text Colors
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Map Line
  static const Color trackPolyline = Color(0xFF10B981);
  static const Color trackPolylineGlow = Color(0x8034D399);
}
